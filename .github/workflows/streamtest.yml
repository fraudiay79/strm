name: run streams.py

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
            python-version: '3.x'

      - name: Install Google Chrome
        run: |
          sudo apt update
          sudo apt install -y google-chrome-stable

      - name: Install ChromeDriver
        run: |
          sudo apt install -y chromium-chromedriver
          sudo chmod +x /usr/bin/chromedriver  # Ensure ChromeDriver is executable

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          
          pwd
          cd ./platforms/

          pip3 install -r requirements.txt
          pip3 install beautifulsoup4
          pip3 install selenium
          pip3 install webdriver-manager
          pip3 install setuptools
          pip3 install undetected-chromedriver
          pip3 install selenium-stealth
      
      - name: execute py script 
        run:  |
            pwd
            cd ./platforms/
            python3 testgin.py > links/testgin.m3u8

      - name: commit and push files
        run: |
          git config --local user.email ""
          git config --local user.name "Stream Ninja 🥷🏽"
          git add -A
          git commit -m "updated streams"
          git push
