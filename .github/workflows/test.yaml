name: epg Test

on:
  workflow_dispatch:

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Set up Node
      uses: actions/setup-node@v4
      with:
           node-version: 18

    - name: npm grab epg
      run: |
          npm install
          #npm test --- primetel.com.cy
          #npm run channels:parse --- --config=./sites/primetel.com.cy/primetel.com.cy.config.js --output=./sites/primetel.com.cy/primetel.com.cy.channels.xml
          npm run grab --- --site=primetel.com.cy --maxConnections=20 --output=guides/primetel.com.cy.xml --days=1 --timeout=90000
   
    - name: Commit changes
      run: |
        git config --local user.email "your-email@example.com"
        git config --local user.name "Your Name"
        git add -f guides/primetel.com.cy.xml
        #git add -f sites/primetel.com.cy/primetel.com.cy.channels.xml
        git commit -m "Update xml"
        git push
